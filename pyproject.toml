[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pysommer"
version = "0.1.0"
description = "Python translation of core computational routines from sommer"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "numpy>=1.23",
  "scipy>=1.10",
]

[dependency-groups]
dev = [
  "ipykernel>=6.0",
  "jupyterlab>=4.0",
  "pytest>=7.0",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
include = ["pysommer*"]
